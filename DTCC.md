# dtcc —— 用手机钉钉遥控这台电脑上的 Claude Code

跟 dtwatch 是两回事：
**dtwatch 管别人发给我的消息**，**dtcc 管我和自己电脑之间的对话**。
两者共用「我自己」那个钉钉单聊窗口（钉钉里给自己发消息的那个会话），但互不干扰 ——
dtwatch 的采集器明确跳过「自己发的消息」（`dtwatch.py:305`），
所以这个窗口里的来回永远不会进它的待办队列。

## 它解决什么

人离开工位以后，Claude Code 在电脑上照常跑，但两件事断了：

1. 不知道它干到哪了；
2. 它需要一个决定（继续 / 停 / 选哪个方案）时，没人回答，就一直挂着。

dtcc 把这个自聊天窗口变成 Claude Code 的**第二块屏幕 + 第二个键盘**。

## 方向和前缀

会话里所有消息的 sender 都是「我自己」—— 脚本发的和手机打的，
接口看起来一模一样。所以靠**文本前缀**区分方向，别改这个约定：

| 谁 | 前缀 | 例子 |
|---|---|---|
| 脚本 → 手机 | `【CC】` `【CC?】` `【dtwatch】` | `【CC】改完了，测试全过` |
| 手机 → 脚本 | `>` （`＞` `》` 也认） | `>继续` |

### 什么算指令

两条独立的路，满足**任一条**就算：

| 手机上的动作 | 算指令？ | 为什么 |
|---|---|---|
| 点 `【CC】` 消息的「回复」，随便说 | ✅ | 引用了 CC 的消息，就是明确在跟电脑说话 |
| `>` 开头，不引用任何东西 | ✅ | 显式前缀 |
| 裸发一句话，不引用也不加 `>` | ❌ | 当成写给自己的便签 |
| 引用**别人**的消息（转发/别人发的） | ❌ | 要加 `>` |

所以 `>` 没被取代，它管的是「没有那条消息可引用」的场合 ——
路上突然想到一件事主动发，或者想引用别人的话再让它干活。

例外：脚本正在等一个 `ask` 的答复时，裸回也算答案（回数字选项也行）。

内建指令（`status` / `remote` / `help`）必须**整句就是它**才生效。
用 `startswith` 匹配会把正常说话吃掉 —— 引用播报回一句「遥控台挺好用的」
不该变成开窗口指令，那句话得原样交给 Claude。

## 引用回复：搞不混 context 的关键

接口在每条消息上给了 `quotedMessage` 字段，里面是被引用那条的完整原文 + message id。
**这个字段必须一路带到 Claude 面前**，否则同一个窗口里同时挂着好几件事的时候，
指令必然对错人。

真出过一次：手机上引用「某人追问」那条播报，回了一句只有代词、没有主语的指令
（大意是「回他一句话」），第一版代码丢了 `quotedMessage`，
「他」就没有了指代对象 —— Claude 猜成了另一个人，还多问了一轮。

所以现在：

- `collect()` 把 `quoted_id` / `quoted_time` / `quoted_text` 一起返回；
- Stop hook 注入指令时，reason 里明写「他引用的是你之前这条播报，别套到别的事上」；
- `ask` 的答复如果引用的不是刚问的那个问题，返回里带一条 `warning`；
- `tail` 用 `↳ 引用 …` 把关系画出来。

## 手机上能发什么

最省事的用法：**直接点那条播报的「回复」按钮说话**，想加 `>` 也行，不加也认。

```
>继续                      让它接着干
>停，先别改了               让它收手
>2                        选第二个方案（回答 【CC?】 提的问题）
>跑一下测试再告诉我结果       任何自然语言指令，直接变成下一轮的 prompt
>status                   现在在干什么
>remote 90                打开遥控窗口 90 分钟
>remote off               关掉
>help                     用法
```

## 电脑这侧的命令

```bash
python3 dtcc.py remote on 90        # 出门前打开遥控窗口（关键开关，见下）
python3 dtcc.py remote off
python3 dtcc.py remote status

python3 dtcc.py say "改完了，正在跑测试"          # 主动播报一条
python3 dtcc.py ask "要不要顺手提交？" \
        --options "提交|先别提交|只提交后端" --timeout 540   # 阻塞等答复，返回 JSON
python3 dtcc.py wait --timeout 540                # 阻塞等任意一条指令
python3 dtcc.py inbox [--peek]                    # 取走攒下的指令，不阻塞
python3 dtcc.py tail --hours 6                    # 看最近的来回
```

`ask` / `wait` 是**阻塞**的：进程挂在那儿轮询，等的时候不烧 token，
这是让会话「保持在线听指令」最便宜的办法。
Bash 工具单次上限 10 分钟，所以 `--timeout` 别超过 540 秒，不够就再调一次。

## 遥控窗口：一个开关决定吵不吵

`remote` 这个开关是整套东西的总闸，默认**关**：

| | 遥控窗口 **关**（默认） | 遥控窗口 **开** |
|---|---|---|
| 每轮干完 | 一句话都不发 | 自动把最后那段话推到手机 |
| 需要授权 / 空等输入 | 不推 | 推一条到手机 |
| 每轮干完等指令 | 只静默探一次队列 | 最多挂着等 10 分钟 |

关着也要探一次队列，否则手机上发的 `>remote 90` 永远没人接，
遥控模式就没法从手机那侧打开了。探一次约 1～2 秒，不发任何消息。

## 三条入口，都走同一个队列

1. **Stop hook**（`~/.claude/settings.json` 里的 `Stop`，结构见
   [`settings.example.json`](settings.example.json)）——
   Claude 每轮活干完时被调用：播报这一轮说了什么，然后看有没有新指令。
   有 → 返回 `{"decision":"block","reason":"<手机上那条指令>"}`，
   Claude 就带着这条指令继续干；没 → 放它停下。
   **这是"是否继续干活"真正的实现点**：手机变成了 stdin。
   注意 `stop_hook_active` 会被尊重 —— 被 hook 续过一轮就不再连环续，不会死循环。
2. **Notification hook** —— 要授权、或空等输入超过 60 秒时捅一下手机。
3. **Claude 主动调 `ask` / `wait`** —— 它知道自己需要一个决定的时候，
   不要用终端里的 AskUserQuestion（手机上看不见），改用 `dtcc ask`。

## 配置

`config.json` 的 `cc` 段：

| 键 | 默认 | 作用 |
|---|---|---|
| `broadcast_on_stop` | true | 遥控窗口开着时，每轮收尾自动播报 |
| `stop_wait_seconds` | 0 | 窗口关着时收尾等几秒（0 = 只探一次） |
| `remote_wait_seconds` | 600 | 窗口开着时收尾最多挂多久 |
| `default_remote_minutes` | 90 | `remote on` 不带参数时的窗口长度 |
| `poll_interval` | 6 | 等指令的轮询间隔（秒） |
| `notify_always` | false | 窗口关着也推授权提醒 |

改 `remote_wait_seconds` 要同步改 `~/.claude/settings.json` 里
那条 Stop hook 的 `timeout`（要比它大一点，模板里给的是 640）。

## 状态文件

```
data/cc/state.json   游标 / 已消费消息 ID / 遥控窗口到期时间 / 上次播报
data/cc/cc.log       发出去和收到的每一条
```

## 已知边界

- **钉钉侧没有 push，只能轮询**（跟 dtwatch 一样的老问题）。
  等指令时 6 秒一轮，所以手机发出去到电脑反应，最坏差一个轮询间隔。
- **`list_conversation_message_v2` 会偶发 `AUTH_PERMISSION_DENIED`**，
  不是配置错了 —— 同样的参数隔几秒再打就成功。`fetch()` 里重试两次（间隔 3 秒）；
  三次都失败就返回空，指令留在钉钉里下一轮再拿，不会丢。
  日志里 `[fetch] tryN err=...` 就是这个。
- **Stop hook 的长等待已经实测过**：settings.json 里给了 `timeout: 640`，
  实际跑到 `waited=505s` 才收到指令，没被 harness 掐断
  （hook 默认 timeout 只有 60 秒，所以这个字段是必须写的）。
  真被掐断的话表现是「等待提前结束、指令留在队列里下一轮才拿走」，不会丢；
  `data/cc/cc.log` 里 `[hook-stop] budget=Ns waited=Ms` 一比就知道。
- **hook 只在有 Claude Code 会话活着的时候起作用**。会话关了，
  手机上发的指令留在钉钉里不丢，下次开会话第一轮收尾就会被拿走。
- **一台电脑一个通道**。多个 Claude Code 会话同时开着，
  谁先探到指令谁拿走 —— 出门前只留一个会话。
