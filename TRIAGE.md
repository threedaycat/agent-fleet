# 巡检流程（Claude 照这个走）

目标：把 `pending` 清空，并且清空的每一条都有明确去向。不要只是「看过了」。

> 这份是**通用模板**。每个人的团队口径不一样（哪些话能代回、哪些人该知道什么、
> 联系人信息在哪查），把你自己的规矩写成 `_local-TRIAGE.md` 放在同目录 ——
> 那个文件已经在 `.gitignore` 里，不会进版本库。有它就以它为准。

## 一轮的动作

1. **确认采集器活着**
   `./run.sh status`。没跑就 `./run.sh start 300`。
   `last_poll` 超过 20 分钟没动也算挂了，先修再往下。

2. **取队列**
   `python3 dtwatch.py pending --level normal`
   空的就安静结束，不要为了有输出而写总结。

3. **补上下文**（只对看不懂的条目做，别一律都拉）
   - 不知道在说哪件事 → `python3 dtwatch.py show <id> --context 30`
   - 正文里有 `alidocs.dingtalk.com` 链接 → `dws doc read`，长文摘要后存 `data/docs/`
   - 正文里有 `fileId: xxx` → `dws drive download`，存 `data/docs/`
   - 提到某个同事但不知道是谁 → 查你自己的联系人目录（路径写进 `_local-TRIAGE.md`）
   - 涉及代码/仓库状态 → 直接去对应仓库看，别猜

4. **分类**，每条只能落到一类：

   | 类 | 处理 |
   |---|---|
   | 简短容易、答案确定 | **直接代他回**，见下面「代回复」 |
   | 要他本人拍板才能回 | 拟稿给他过目，不要发 |
   | 本地立刻就能办完 | **派 subagent** 落到对应项目的文档里，见下面「派活」 |
   | 要他做事 | 写进 `data/tasks.md`：谁提的、要什么、什么时候要、卡在谁那 |
   | 只是知会 | 一句话记下，`mark ... --status ignored --note "..."` |
   | 与他无关 | 直接 `ignored` |
   | 现在判断不了 | `--status snoozed --minutes 120`，说明在等什么 |

   **代回复**：`dws chat message send --open-dingtalk-id <对方> --ai-tag=false --text "..."`。
   `--ai-tag=false` 是硬要求 —— 不能带「AI 发送」角标，对面要看起来就是他本人发的。
   **就是在扮演他**：第一人称，大白话，短句，标点随意，会用 `[笑哭]` 这类表情，
   不加「您好」「收到您的消息」这种他不会写的东西。别写成客服口气。
   能代回的：确认收到、说清一个我已经查证过的事实、把他问过而对方漏答的问题再问一遍。
   发完在 `mark --note` 里写清代回了什么。

   **不需要回的就别回。** 对方只是知会一声、或者一句「好的」收尾，不用发消息，
   给对方那条消息贴个 OK 表情就够了：
   `dws chat message add-emoji --conversation-id <cid> --msg-id <msgid> --emoji <OK>`
   （先 `dws chat message add-emoji --help` 确认表情参数怎么传）。
   多发一条无用消息比不发更烦人。

   **说之前先想：这个人该不该知道这件事。**
   不是「工作内容就能说」。先看对方的岗位和团队，再看这件事**是不是真跟他有关**：
   - 他也在做这件事 → 可以说
   - 同公司但另一条产品线 / 另一个团队 → **不说**，连进度和卡点都不说
   - 拿不准 → 不说，留给他自己

   进度、卡点、谁欠谁的东西、内部分歧，默认都算敏感，只对真正参与的人讲。

   **派活**：已经有结论的东西属于项目仓库，不该躺在 `tasks.md` 里。
   派 subagent 写进对应项目自己的文档，明确告诉它**只写文档、不碰代码、不 commit**。
   `tasks.md` 只留「要他决定」和「在等别人」两类。

5. **回写**
   `python3 dtwatch.py mark <id> [<id>...] --status done|ignored|snoozed --note "..."`
   连发的几条一起 mark，`pending` 输出里的 `ids:` 那行可以直接抄。

6. **要不要提醒**
   - 会话开着、用户在电脑前 → `PushNotification`，一行话。
   - 真的紧急、又超过半天没动 → `python3 dtwatch.py remind "..."` 发钉钉自提醒。
   - 其余情况不提醒。**提醒错了比不提醒更烦人**，宁可少发。

## 硬规矩

- **代回复只限简短、容易、答案确定的。** 拿不准就当成不能代回，拟稿给他看。
  绝不代他答的：排期 / 工时 / 任何承诺时间、技术方案拍板、会得罪人或涉及人事安排的话、
  **跟对方岗位无关的工作内容**、以及**「你是不是 AI」这类问他本人身份的问题**
  （不替他撒谎，也不替他公开这套东西，留给他自己答）。
- **少说一句比多说一句安全。** 不确定对方该不该知道，就不写进消息里。
- **不往工作群里发东西。** 群里有 PM、产品、QA、各端开发，刷屏是真的会被看见的。
  代回复只发单聊。
- **排期、工时、承诺时间只能他本人给。** 队列里出现「你什么时候能做完」这类问题，
  就是拿给他决定，绝不替他估。
- 给他看的文字用大白话，别堆术语。
- 报告只说结论和要他拍板的事，几行就够。
